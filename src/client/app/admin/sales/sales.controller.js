(function() {
  'use strict';

  angular
  .module('app.dashboard')
  .controller('Sales', Sales);

  Sales.$inject = ['$scope'];

  function Sales($scope) {
    var shell = $scope.shell;
    var sales = this;

    
  }

})();
