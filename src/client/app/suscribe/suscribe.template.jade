.section-title.
  Suscripciones
.search-box.pure-g
  input(type="text", ng-model="vm.searchValue")
  a(ng-click="vm.searchByName()").
    BUSCAR
.shelf(data-ng-include="'app/layout/shelf.template.html'", data-ng-if="vm.itemsList.length > 0")
.shelf(shelf, publisher="dc")
.shelf(shelf, publisher="marvel")
